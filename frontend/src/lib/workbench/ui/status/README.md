# status

`StatusBar` fills the shell's status area with start and end groups. `StatusItem`
presents one compact indicator as text, an icon, or a busy spinner, becoming a
button when given `onclick`. Announcements for active work and notifications
remain with the consuming host; this layer does not subscribe to services.
