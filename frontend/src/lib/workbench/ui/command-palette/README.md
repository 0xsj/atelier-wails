# command-palette

`CommandPalette` presents a supplied command list through `QuickPick`: it
filters, supports arrow-key navigation and Enter execution, and reports the
selected command through a callback. The registry and dispatch contract remain
owned by the workbench host.
